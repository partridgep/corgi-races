export const NotFoundPage = () => {
    return (
        <div>
            <h1>Your corgi raced to the wrong spot!</h1>
            <p>It looks like this page doesn't exist.</p>
        </div>
    )
}